const nav=document.getElementById('nav'),ov=document.getElementById('overlay'),dash=document.getElementById('dashboard'),mod=document.getElementById('module'),st=document.getElementById('status');
const money=v=>'PKR '+Number(v||0).toLocaleString('en-PK',{maximumFractionDigits:0});
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const api='/api/data';let current='dashboard',records=[],editing=null,cache={};
const defs={
 clients:{title:'Clients',resource:'clients',cols:[['business_name','Business'],['contact_person','Contact'],['mobile_number','Mobile'],['credit_limit','Credit Limit','money'],['opening_balance','Opening Balance','money'],['status','Status','status']],fields:[['business_name','Business Name','text',1],['contact_person','Contact Person','text'],['mobile_number','Mobile Number','tel'],['whatsapp_number','WhatsApp Number','tel'],['address','Address','textarea'],['credit_limit','Credit Limit','number'],['opening_balance','Opening Balance','number'],['notes','Notes','textarea'],['status','Status','select:active,inactive']]},
 suppliers:{title:'Suppliers',resource:'suppliers',cols:[['business_name','Business'],['contact_person','Contact'],['mobile_number','Mobile'],['whatsapp_number','WhatsApp'],['opening_balance','Opening Balance','money'],['status','Status','status']],fields:[['business_name','Business Name','text',1],['contact_person','Contact Person','text'],['mobile_number','Mobile Number','tel'],['whatsapp_number','WhatsApp Number','tel'],['address','Address','textarea'],['opening_balance','Opening Balance','number'],['notes','Notes','textarea'],['status','Status','select:active,inactive']]},
 'supplier-bills':{title:'Supplier Bills',resource:'supplier_invoices',party:'supplier',cols:[['invoice_number','Invoice'],['business_name','Supplier'],['invoice_date','Date'],['due_date','Due'],['amount','Amount','money'],['status','Status','status']],fields:[['supplier_id','Supplier','supplier',1],['invoice_number','Invoice Number','text'],['invoice_date','Invoice Date','date'],['due_date','Due Date','date'],['amount','Amount','number',1],['attachment_url','Attachment URL','url'],['notes','Notes','textarea'],['status','Status','select:unpaid,partial,paid']]},
 'supplier-payments':{title:'Supplier Payments',resource:'supplier_payments',party:'supplier',cols:[['payment_date','Date'],['business_name','Supplier'],['amount','Amount','money'],['payment_method','Method'],['bank','Bank'],['reference_number','Reference']],fields:[['supplier_id','Supplier','supplier',1],['payment_date','Payment Date','date'],['amount','Amount','number',1],['payment_method','Method','select:CASH,BANK,ONLINE,CHEQUE'],['bank','Bank','text'],['reference_number','Reference Number','text'],['attachment_url','Attachment URL','url'],['notes','Notes','textarea']]},
 'client-sales':{title:'Client Bills',resource:'client_invoices',party:'client',cols:[['invoice_number','Invoice'],['business_name','Client'],['invoice_date','Date'],['due_date','Due'],['amount','Amount','money'],['status','Status','status']],fields:[['client_id','Client','client',1],['invoice_number','Invoice Number','text'],['invoice_date','Invoice Date','date'],['due_date','Due Date','date'],['amount','Amount','number',1],['attachment_url','Attachment URL','url'],['notes','Notes','textarea'],['status','Status','select:unpaid,partial,paid']]},
 'client-receipts':{title:'Client Payments',resource:'client_receipts',party:'client',cols:[['receipt_date','Date'],['business_name','Client'],['amount','Amount','money'],['payment_method','Method'],['bank','Bank'],['reference_number','Reference']],fields:[['client_id','Client','client',1],['receipt_date','Receipt Date','date'],['amount','Amount','number',1],['payment_method','Method','select:CASH,BANK,ONLINE,CHEQUE'],['bank','Bank','text'],['reference_number','Reference Number','text'],['attachment_url','Attachment URL','url'],['notes','Notes','textarea']]},
 documents:{title:'Documents',resource:'documents',cols:[['document_type','Type'],['entity_type','Entity'],['entity_id','Entity ID'],['file_name','File Name'],['file_url','File URL','link'],['created_at','Created']],fields:[['entity_type','Entity Type','select:SUPPLIER,CLIENT,OTHER'],['entity_id','Entity ID','number'],['document_type','Document Type','text',1],['file_name','File Name','text'],['file_url','File URL','url',1],['mime_type','MIME Type','text']]}
};
function shut(){nav.classList.remove('open');ov.classList.remove('show')}
document.getElementById('hamb').onclick=()=>{nav.classList.add('open');ov.classList.add('show')};document.getElementById('close').onclick=shut;ov.onclick=shut;
function toast(t,bad=false){let x=document.getElementById('toast');if(!x){x=document.createElement('div');x.id='toast';x.className='toast';document.body.appendChild(x)}x.textContent=t;x.style.background=bad?'#8d3d32':'#173f35';x.classList.remove('hidden');clearTimeout(x._t);x._t=setTimeout(()=>x.classList.add('hidden'),2400)}
async function request(resource,opt={}){let url=api+'?resource='+encodeURIComponent(resource);if(opt.id)url+='&id='+opt.id;const r=await fetch(url,{cache:'no-store',headers:{'Content-Type':'application/json'},...opt});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||('HTTP '+r.status));return j}
async function home(){current='dashboard';dash.classList.remove('hidden');mod.classList.add('hidden');try{const r=await fetch('/api/dashboard',{cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||'Database error');st.textContent='Database Connected';document.getElementById('stats').innerHTML=`<div class="stat"><small>Client Receivables</small><strong>${money(d.totalClientReceivable)}</strong><small>${d.clientCount||0} clients</small></div><div class="stat"><small>Supplier Payables</small><strong>${money(d.totalSupplierPayable)}</strong><small>${d.supplierCount||0} suppliers</small></div><div class="stat"><small>Supplier Purchases</small><strong>${money(d.totalSupplierPurchases)}</strong><small>Recorded bills</small></div><div class="stat"><small>Client Credit Sales</small><strong>${money(d.totalClientSales)}</strong><small>Recorded sales</small></div>`;document.getElementById('overview').innerHTML=`Supplier payments: <b>${money(d.totalSupplierPayments)}</b> &nbsp;•&nbsp; Client payments: <b>${money(d.totalClientReceipts)}</b> &nbsp;•&nbsp; Suppliers: <b>${d.supplierCount||0}</b> &nbsp;•&nbsp; Clients: <b>${d.clientCount||0}</b>`}catch(e){st.textContent='Database Offline';toast(e.message,true)}}
function format(v,type){if(v===null||v===undefined||v==='')return '—';if(type==='money')return `<span class="money">${money(v)}</span>`;if(type==='status'){const s=String(v);const c=/paid|active/i.test(s)?'badge':/partial/i.test(s)?'badge warn':'badge bad';return `<span class="${c}">${esc(s)}</span>`}if(type==='link')return `<a class="link" href="${esc(v)}" target="_blank" rel="noopener">Open</a>`;return esc(v)}
async function loadModule(view){current=view;editing=null;dash.classList.add('hidden');mod.classList.remove('hidden');const d=defs[view];if(!d)return;document.getElementById('mt').textContent=d.title;document.getElementById('mp').textContent='Live Kashif Traders records';mod.innerHTML=`<div class="modulehead"><div><h2 id="mt">${d.title}</h2><p>Live Kashif Traders records</p></div><button class="btn" id="addBtn">+ Add Record</button></div><div class="toolbar"><input id="filter" class="searchbox" placeholder="Search ${d.title.toLowerCase()}..."><span class="spacer"></span><button class="btn light" id="refreshBtn">Refresh</button><button class="btn alt" id="printBtn">Print</button></div><div class="tablewrap"><table><thead><tr>${d.cols.map(c=>`<th>${c[1]}</th>`).join('')}<th>Actions</th></tr></thead><tbody id="tbody"><tr><td colspan="${d.cols.length+1}" class="empty">Loading...</td></tr></tbody></table></div>`;document.getElementById('addBtn').onclick=()=>openForm();document.getElementById('refreshBtn').onclick=()=>loadModule(view);document.getElementById('printBtn').onclick=()=>window.print();document.getElementById('filter').oninput=e=>renderRows(e.target.value);try{records=(await request(d.resource)).records||[];cache[d.resource]=records;renderRows('')}catch(e){document.getElementById('tbody').innerHTML=`<tr><td colspan="${d.cols.length+1}" class="empty">${esc(e.message)}</td></tr>`;toast(e.message,true)}}
function renderRows(q){const d=defs[current];const list=!q?records:records.filter(r=>Object.values(r).some(v=>String(v??'').toLowerCase().includes(q.toLowerCase())));document.getElementById('tbody').innerHTML=list.length?list.map(r=>`<tr>${d.cols.map(c=>`<td>${format(r[c[0]],c[2])}</td>`).join('')}<td><div class="rowactions"><button class="btn small light" onclick="editRecord(${r.id})">Edit</button>${d.party?`<button class="btn small alt" onclick="statementFor(${r[d.party+'_id']||r.id},'${d.party}')">Statement</button>`:''}<button class="btn small danger" onclick="deleteRecord(${r.id})">Delete</button></div></td></tr>`).join(''):`<tr><td colspan="${d.cols.length+1}" class="empty">No records yet</td></tr>`}
async function partyOptions(type){const resource=type==='supplier'?'suppliers':'clients';if(!cache[resource])cache[resource]=(await request(resource)).records||[];return cache[resource]}
async function openForm(row=null){editing=row?.id||null;const d=defs[current];let modal=document.getElementById('modal');if(modal)modal.remove();modal=document.createElement('div');modal.id='modal';modal.className='modal';let fields='';for(const f of d.fields){const [name,label,type,req]=f;const val=row?.[name]??'';let input='';if(type==='textarea')input=`<textarea name="${name}" ${req?'required':''}>${esc(val)}</textarea>`;else if(type.startsWith('select:'))input=`<select name="${name}" ${req?'required':''}>${type.split(':')[1].split(',').map(x=>`<option value="${esc(x)}" ${String(val)===x?'selected':''}>${esc(x)}</option>`).join('')}</select>`;else if(type==='supplier'||type==='client'){const opts=await partyOptions(type);input=`<select name="${name}" ${req?'required':''}><option value="">Select</option>${opts.map(x=>`<option value="${x.id}" ${String(val)===String(x.id)?'selected':''}>${esc(x.business_name)}</option>`).join('')}</select>`}else input=`<input name="${name}" type="${type}" value="${esc(val)}" ${type==='number'?'step="0.01"':''} ${req?'required':''}>`;fields+=`<div class="field ${type==='textarea'?'full':''}"><label>${label}</label>${input}</div>`}modal.innerHTML=`<div class="box"><h3>${editing?'Edit':'Add'} ${d.title.replace(/s$/,'')}</h3><form id="recordForm"><div class="grid">${fields}</div><div class="actions"><button type="button" class="btn alt" id="cancelBtn">Cancel</button><button type="submit" class="btn">Save</button></div></form></div>`;document.body.appendChild(modal);document.getElementById('cancelBtn').onclick=()=>modal.remove();document.getElementById('recordForm').onsubmit=saveForm}
async function saveForm(e){e.preventDefault();const d=defs[current],b=Object.fromEntries(new FormData(e.target).entries());try{await request(d.resource,{method:editing?'PATCH':'POST',id:editing||undefined,body:JSON.stringify(b)});document.getElementById('modal')?.remove();cache={};toast(editing?'Updated':'Saved');await loadModule(current);await refreshDashboardSilent()}catch(err){toast(err.message,true)}}
window.editRecord=id=>{const row=records.find(x=>Number(x.id)===Number(id));if(row)openForm(row)};
window.deleteRecord=async id=>{if(!confirm('Delete this record?'))return;try{await request(defs[current].resource,{method:'DELETE',id});cache={};toast('Deleted');await loadModule(current);await refreshDashboardSilent()}catch(e){toast(e.message,true)}};
async function statementFor(id,type){const partyRes=type==='supplier'?'suppliers':'clients';const billRes=type==='supplier'?'supplier_invoices':'client_invoices';const payRes=type==='supplier'?'supplier_payments':'client_receipts';try{const [p,bills,pays]=await Promise.all([request(partyRes,{id}),request(billRes),request(payRes)]);const party=p.records?.[0];if(!party)throw new Error('Party not found');const billRows=(bills.records||[]).filter(x=>Number(x[type+'_id'])===Number(id)),payRows=(pays.records||[]).filter(x=>Number(x[type+'_id'])===Number(id));const charges=Number(party.opening_balance||0)+billRows.reduce((s,x)=>s+Number(x.amount||0),0),paid=payRows.reduce((s,x)=>s+Number(x.amount||0),0);dash.classList.add('hidden');mod.classList.remove('hidden');mod.innerHTML=`<div class="toolbar"><button class="btn light" onclick="loadModule('${type==='supplier'?'suppliers':'clients'}')">← Back</button><span class="spacer"></span><button class="btn alt" onclick="window.print()">Print Statement</button></div><div class="statement"><h3>${esc(party.business_name)} Statement</h3><div class="summaryline"><span>Opening: <b>${money(party.opening_balance)}</b></span><span>Total bills: <b>${money(billRows.reduce((s,x)=>s+Number(x.amount||0),0))}</b></span><span>Payments: <b>${money(paid)}</b></span><span>Balance: <b>${money(charges-paid)}</b></span></div></div><div class="panel"><div class="tablewrap"><table><thead><tr><th>Date</th><th>Type</th><th>Reference</th><th>Debit</th><th>Credit</th></tr></thead><tbody>${billRows.map(x=>`<tr><td>${esc(x.invoice_date||'')}</td><td>Bill</td><td>${esc(x.invoice_number||'')}</td><td class="money">${money(x.amount)}</td><td>—</td></tr>`).join('')}${payRows.map(x=>`<tr><td>${esc(x.payment_date||x.receipt_date||'')}</td><td>Payment</td><td>${esc(x.reference_number||'')}</td><td>—</td><td class="money">${money(x.amount)}</td></tr>`).join('')||'<tr><td colspan="5" class="empty">No transactions yet</td></tr>'}</tbody></table></div></div>`}catch(e){toast(e.message,true)}}
window.statementFor=statementFor;
async function showSearch(){current='search';dash.classList.add('hidden');mod.classList.remove('hidden');mod.innerHTML=`<div class="modulehead"><div><h2>Search</h2><p>Search across suppliers, clients, bills and payments</p></div></div><div class="toolbar"><input id="globalSearch" class="searchbox" placeholder="Type business name, invoice, reference or phone..."></div><div id="searchResults" class="panel"><div class="empty">Start typing to search all records</div></div>`;document.getElementById('globalSearch').oninput=async e=>{const q=e.target.value.trim().toLowerCase();if(q.length<2){document.getElementById('searchResults').innerHTML='<div class="empty">Type at least 2 characters</div>';return}const resources=['suppliers','clients','supplier_invoices','supplier_payments','client_invoices','client_receipts','documents'];try{const all=(await Promise.all(resources.map(r=>request(r).then(x=>(x.records||[]).map(y=>({...y,_resource:r}))))).flat();const hits=all.filter(x=>Object.values(x).some(v=>String(v??'').toLowerCase().includes(q))).slice(0,100);document.getElementById('searchResults').innerHTML=hits.length?`<div class="tablewrap"><table><thead><tr><th>Type</th><th>Name / Ref</th><th>Details</th></tr></thead><tbody>${hits.map(x=>`<tr><td>${esc(x._resource.replaceAll('_',' '))}</td><td>${esc(x.business_name||x.invoice_number||x.reference_number||x.file_name||('ID '+x.id))}</td><td>${esc(x.mobile_number||x.amount||x.document_type||x.status||'')}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No matching records</div>'}catch(err){toast(err.message,true)}}}
async function showReports(){current='reports';dash.classList.add('hidden');mod.classList.remove('hidden');try{const [d,s,c,si,sp,ci,cr]=await Promise.all([fetch('/api/dashboard',{cache:'no-store'}).then(r=>r.json()),request('suppliers'),request('clients'),request('supplier_invoices'),request('supplier_payments'),request('client_invoices'),request('client_receipts')]);mod.innerHTML=`<div class="modulehead"><div><h2>Reports & Analytics</h2><p>Live business summary</p></div><button class="btn alt" onclick="window.print()">Print Report</button></div><div class="reportgrid"><div class="reportcard"><small>Supplier Payable</small><strong>${money(d.totalSupplierPayable)}</strong></div><div class="reportcard"><small>Client Receivable</small><strong>${money(d.totalClientReceivable)}</strong></div><div class="reportcard"><small>Supplier Purchases</small><strong>${money(d.totalSupplierPurchases)}</strong></div><div class="reportcard"><small>Supplier Payments</small><strong>${money(d.totalSupplierPayments)}</strong></div><div class="reportcard"><small>Client Sales</small><strong>${money(d.totalClientSales)}</strong></div><div class="reportcard"><small>Client Payments</small><strong>${money(d.totalClientReceipts)}</strong></div></div><div class="panel"><h2>Record Counts</h2><div class="summaryline"><span>Suppliers <b>${s.records.length}</b></span><span>Clients <b>${c.records.length}</b></span><span>Supplier bills <b>${si.records.length}</b></span><span>Supplier payments <b>${sp.records.length}</b></span><span>Client bills <b>${ci.records.length}</b></span><span>Client payments <b>${cr.records.length}</b></span></div></div>`}catch(e){toast(e.message,true)}}
function showSettings(){current='settings';dash.classList.add('hidden');mod.classList.remove('hidden');mod.innerHTML=`<div class="modulehead"><div><h2>Settings</h2><p>Kashif Traders workspace information</p></div></div><div class="twocol"><div class="panel"><h2>Business</h2><div class="overview"><b>Kashif Traders</b><br>Trading & Accounts Workspace<br>Currency: PKR</div></div><div class="panel"><h2>Database</h2><div class="overview">Connection: <b>${esc(st.textContent)}</b><br>Provider: Neon PostgreSQL<br>Environment: Production</div></div></div><div class="panel"><h2>System Notes</h2><div class="overview">This workspace uses the approved green, cream and gold Kashif Traders design. Core records are stored in the dedicated Kashif Traders database.</div></div>`}
async function refreshDashboardSilent(){try{const r=await fetch('/api/dashboard',{cache:'no-store'});if(r.ok)st.textContent='Database Connected'}catch{}}
function route(v){nav.querySelectorAll('[data-view]').forEach(x=>x.classList.toggle('active',x.dataset.view===v));if(v==='dashboard')home();else if(defs[v])loadModule(v);else if(v==='search')showSearch();else if(v==='reports')showReports();else if(v==='settings')showSettings();shut();window.scrollTo({top:0,behavior:'instant'})}
nav.onclick=e=>{const b=e.target.closest('[data-view]');if(b)route(b.dataset.view)};window.loadModule=loadModule;home();
