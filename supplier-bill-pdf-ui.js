'use strict';
(()=>{
  async function getPdf(id){const r=await fetch('/api/supplier-bill-pdf?id='+encodeURIComponent(id),{cache:'no-store'});if(!r.ok){const j=await r.json().catch(()=>({}));throw new Error(j.error||'Supplier bill PDF failed')}return r.blob()}
  function filename(row){return ((row?.invoice_number||row?.entry_number||('supplier-bill-'+row?.id)).replace(/[^a-z0-9_-]+/gi,'-')||'supplier-bill')+'.pdf'}
  async function generate(row,button){const old=button.textContent;button.disabled=true;button.textContent='Generating…';try{const blob=await getPdf(row.id),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename(row);document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500);if(typeof toast==='function')toast('Supplier bill PDF generated')}catch(e){if(typeof toast==='function')toast(e.message||'Supplier bill PDF failed',true)}finally{button.disabled=false;button.textContent=old}}
  async function share(row,button){const old=button.textContent;button.disabled=true;button.textContent='Preparing…';try{const blob=await getPdf(row.id),file=new File([blob],filename(row),{type:'application/pdf'});if(navigator.share&&(!navigator.canShare||navigator.canShare({files:[file]}))){await navigator.share({files:[file],title:'Supplier Bill '+(row.invoice_number||''),text:'Kashif Traders supplier bill'});if(typeof toast==='function')toast('Supplier bill PDF ready to share');return}const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=file.name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500);if(typeof toast==='function')toast('PDF downloaded — share from your device')}catch(e){if(e?.name!=='AbortError'&&typeof toast==='function')toast(e.message||'Supplier bill share failed',true)}finally{button.disabled=false;button.textContent=old}}
  function enhance(){if(typeof currentView==='undefined'||currentView!=='supplier-bills')return;const body=document.getElementById('tbody');if(!body||typeof currentRows==='undefined')return;body.querySelectorAll('tr').forEach(tr=>{const edit=tr.querySelector('[data-action="edit"]');if(!edit)return;const id=Number(edit.dataset.id),row=currentRows.find(x=>Number(x.id)===id);if(!row)return;const actions=edit.closest('.rowactions');if(!actions||actions.querySelector('[data-supplier-pdf]'))return;const gen=document.createElement('button');gen.className='btn small alt';gen.dataset.supplierPdf='generate';gen.textContent='Generate PDF';gen.addEventListener('click',()=>generate(row,gen));const sh=document.createElement('button');sh.className='btn small';sh.dataset.supplierPdf='share';sh.textContent='Share PDF';sh.addEventListener('click',()=>share(row,sh));actions.insertBefore(gen,edit);actions.insertBefore(sh,edit)})}
  const observer=new MutationObserver(()=>enhance());observer.observe(document.getElementById('module'),{childList:true,subtree:true});setTimeout(enhance,0);
})();
